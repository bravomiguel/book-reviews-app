import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    primary: {
      main: '#CC5500'
    },
    secondary: {
      main: "#0063AF"
    },
  }
});

export default theme;